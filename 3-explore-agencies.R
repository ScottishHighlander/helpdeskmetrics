library(dplyr)

# create data frame for unique tickets
agencies <- as.data.frame(table(metrics.data$Facility))

# what is the difference between this and the "3-explore.R" file?  I'll probably find out if I see the actual dataset but what agencies
# is this speaking of?
